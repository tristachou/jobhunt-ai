Run the full unit test suite and report results.

Execute from the backend directory:
```
cd backend && npm test
```

Then:
- List each test file and its pass/fail count
- For any failing test: show the error message and diagnose the root cause
- If a failure is clearly a code bug (not a test bug), fix it and re-run
- Do not modify tests to make them pass artificially
